[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "20"

# Redirects for single-page routing / client fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
